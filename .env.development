# zukan-server 地址（含协议，末尾无斜杠）
#
# 这里放**对所有人都可用**的默认值，不要写某台机器的局域网 IP —— 换机器 /
# 换网段的人 clone 下来就连不上，改完又变成待提交状态反复污染 git status。
#
# 需要手机连本机调试（手机访问不到 localhost）或需要连远端服务时，在
# `.env.development.local` 里覆盖 —— 该文件已被 .gitignore 的 `*.local` 忽略：
#
#   VITE_API_BASE_URL=http://192.168.x.x:8080
VITE_API_BASE_URL=http://localhost:8080
